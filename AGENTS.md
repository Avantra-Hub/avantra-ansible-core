# Agent Rules

## RULE: Pre-push checklist

**Do not push without checking `docs/pre_push_check.md`.**

Before pushing any changes to the remote, review and follow the pre-push checklist
in `docs/pre_push_check.md`. This includes running `ansible-lint`, sanity tests,
unit tests, and verifying git hygiene.
